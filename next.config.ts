// you can customize your next.js build and development settings here
// Next automatically loads this config before building/running
// so any field you add (the NextConfig you typed) is respected across dev, build, and start
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;

//sample config for allowing images from unsplash
// import type { NextConfig } from "next";

// const nextConfig: NextConfig = {
//   images: {
//     remotePatterns: [
//       { protocol: "https", hostname: "images.unsplash.com" },
//     ],
//   },
// };

// export default nextConfig;
